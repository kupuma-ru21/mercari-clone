export const textCheck = (text: string): boolean => {
  return text === 'カテゴリー 一覧' || text === 'ブランド一覧';
};

export const categoryListTextCheck = (text: string): boolean => {
  return text === 'カテゴリー 一覧';
};
